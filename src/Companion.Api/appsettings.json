{
  "Urls": "http://localhost:5266",
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning",
      "Companion": "Warning"
    }
  },
  "Database": {
    "Path": "companion.db"
  },
  "Models": {
    "//": "Defaults to offline mocks so the API runs with no model server. To use a real model,",
    "//use": "set Provider to 'OpenAiCompatible' and fill in the endpoints (see the CLI appsettings/README).",
    "Provider": "Mock"
  }
}

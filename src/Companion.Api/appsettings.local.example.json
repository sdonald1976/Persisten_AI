{
  "//": "Copy to appsettings.local.json and fill in. That file is gitignored and is where anything specific to this machine belongs.",
  "//loading": "Loaded in EVERY environment, unlike appsettings.Development.json — so a service started without ASPNETCORE_ENVIRONMENT still picks it up. Environment variables still override it.",

  "//world": "Her world's wire, one port above the game client's, and the same secret as the world's .avaworld-token. Leave Url empty and she simply isn't anywhere.",
  "World": {
    "Url": "ws://127.0.0.1:8738",
    "Token": "the world's .avaworld-token contents"
  },

  "//models": "Handy here too: point at a different machine's Ollama without editing the committed config.",
  "//example": "\"Models\": { \"Chat\": { \"BaseUrl\": \"http://192.168.1.20:11434/v1\" } }"
}

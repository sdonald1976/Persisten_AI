# The serving/training venv, pinned to what every run-1a/1b/1c result was produced
# with (Python 3.10, CUDA 12.6 wheels). Install torch first with its index URL:
#   pip install torch==2.7.1+cu126 --index-url https://download.pytorch.org/whl/cu126
# then: pip install -r requirements-serve.txt
transformers==4.57.6
peft==0.20.0
bitsandbytes==0.50.1
accelerate==1.14.0
safetensors==0.8.0
huggingface_hub==0.36.2
